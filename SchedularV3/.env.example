# SchedularV3 Environment Configuration
# =====================================
# Copy this file to .env and fill in the actual values
# NEVER commit .env file to version control!

# =============================================================================
# JWT Authentication (Required when FastAPI backend is enabled)
# =============================================================================
# This secret key is used to sign and verify JWT tokens
# CRITICAL: Generate a strong, random secret key for production
# 
# To generate a secure secret key, use one of these methods:
#   Python: python -c "import secrets; print(secrets.token_urlsafe(32))"
#   OpenSSL: openssl rand -hex 32
#   Online: https://randomkeygen.com/ (CodeIgniter Encryption Keys)
#
# Security Requirements:
# - Minimum 32 characters
# - Use alphanumeric + special characters
# - Never reuse across environments (dev/staging/prod)
# - Rotate periodically (every 90 days recommended)
# - Store securely (use secret management tools in production)
#
SECRET_KEY=your-super-secret-jwt-key-change-this-in-production-min-32-chars

# =============================================================================
# JWT Configuration (Optional - defaults provided)
# =============================================================================
# JWT token expiration time in minutes
# JWT_EXPIRATION_MINUTES=30

# JWT algorithm (HS256, HS384, HS512 recommended for symmetric keys)
# JWT_ALGORITHM=HS256

# =============================================================================
# Database Configuration (Optional - SQLite default)
# =============================================================================
# SQLite database path (relative to project root)
# DATABASE_PATH=course_scheduler.db

# =============================================================================
# Application Configuration (Optional)
# =============================================================================
# Application environment: development, staging, production
# ENVIRONMENT=development

# Enable debug mode (true/false)
# DEBUG=false

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# =============================================================================
# API Configuration (Optional - when FastAPI backend is enabled)
# =============================================================================
# API server host
# API_HOST=127.0.0.1

# API server port
# API_PORT=8000

# Enable API CORS (true/false)
# API_CORS_ENABLED=true

# Allowed CORS origins (comma-separated)
# API_CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# =============================================================================
# Security Best Practices
# =============================================================================
# 1. Copy this file to .env in the same directory
# 2. Generate a new SECRET_KEY using the methods above
# 3. Set appropriate values for your environment
# 4. NEVER commit .env to git (it's in .gitignore)
# 5. Use different SECRET_KEY values for dev/staging/prod
# 6. Store production secrets in secure vault (AWS Secrets Manager, etc.)
# 7. Rotate SECRET_KEY periodically
# 8. Review access logs if SECRET_KEY is compromised
# 9. Use environment-specific .env files (.env.development, .env.production)
# 10. Document all required variables in this .env.example file
